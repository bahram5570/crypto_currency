import NotFound from "../notFound/NotFound";
import Layout from "../layout/Layout";

const NotFoundPage = () => {
  return (
    <Layout>
      <NotFound />
    </Layout>
  );
};

export default NotFoundPage;
